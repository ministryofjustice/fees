<% content_for :title, "Fee Categories" %>

<div class="row">
  <h1>
    Fee categories
  </h1>
</div>

<div class="row">
  <% @categories.each do |category| %>
    <ul>
      <li>
        <%= link_to category.title, category_path(category.friendly_id) %>
      </li>
    </ul>
  <% end %>
</div>
